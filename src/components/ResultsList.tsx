import React from "react";

interface Props {
  gifs: {};
}
export default function ResultsList({ gifs }: Props) {
  return (
    <div>
      <h2>Results</h2>
      {/* {gifs.map(gif=>(

    ))} */}
    </div>
  );
}
